apiVersion: v1
kind: ConfigMap
metadata:
  name: apache-index
  namespace: project9
data:
  index.html: |
    <!doctype html>
    <html>
      <head>
        <meta charset="utf-8"/>
        <title>Project9 — Apache2 on Kubernetes</title>
      </head>
      <body>
        <h1>Project 9 — Apache2 server on Kubernetes</h1>
        <p>This page is served from an Apache2 pod running on Kubernetes (Docker Desktop).</p>
      </body>
    </html>
