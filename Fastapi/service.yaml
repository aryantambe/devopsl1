apiVersion: v1
kind: Service
metadata:
  name: social-media-service
spec:
  selector:
    app: social-media       
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8080              
      targetPort: 8080         
