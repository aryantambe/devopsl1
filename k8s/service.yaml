apiVersion: v1
kind: Service
metadata:
  name: social-media-service
spec:
  selector:
    app: social-media
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30001
