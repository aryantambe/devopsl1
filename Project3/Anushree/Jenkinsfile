pipeline {
    agent any
    tools {
        maven 'Maven' 
    }
    stages {
        stage('Build') {
            steps {
                dir('Project3/Anushree') {
                    sh 'mvn clean install'
                }
            }
        }
    }
}
