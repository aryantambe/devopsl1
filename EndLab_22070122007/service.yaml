apiVersion: v1
kind: Service
metadata:
  name: newsfeed-app-service
  labels:
    app: newsfeed-app
spec:
  type: NodePort
  selector:
    app: newsfeed-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
